services:
  docker:
    volumes:
      - type: bind
        source: ${PROJECT_ENV_PATH}
        target: /workspace/.env
        read_only: true
  webtop:
    volumes:
      - type: bind
        source: ${PROJECT_ENV_PATH}
        target: /workspace/.env
        read_only: true
